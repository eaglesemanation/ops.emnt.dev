apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  namespace: ingress
spec:
  ingressClassName: ingress-internal-traefik
  rules:
    - host: minio.emnt.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: truenas
                port:
                  number: 9000
    - host: minio-console.emnt.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: truenas
                port:
                  number: 9001
