apiVersion: v1
kind: ConfigMap
metadata:
  name: tailscale-config
  namespace: tailscale
data:
  TS_USERSPACE: "false"
  TS_ROUTES: ${LOCAL_CIDR}
  TS_KUBE_SECRET: tailscale-data
  TS_EXTRA_ARGS: --login-server=https://headscale.emnt.dev
