apiVersion: v1
kind: Service
metadata:
  name: tailscale
  namespace: tailscale
  labels:
    app.kubernetes.io/name: tailscale
    app.kubernetes.io/instance: tailscale
spec:
  type: LoadBalancer
  loadBalancerIP: ${SVC_TAILSCALE_ROUTER}
  selector:
    app.kubernetes.io/name: tailscale
    app.kubernetes.io/instance: tailscale
  ports:
    - name: tailscale
      port: 41641
      targetPort: 41641
      protocol: TCP
