acls:
  - action: accept
    src:
      - "*"
    dst:
      # TODO: Replace with autogroup:internet when available
      - 0.0.0.0/0:*
      - "::/0:*"
      #- tag:shared:*
      #- group:gateways:*
      #- 192.168.25.0/24:*
groups:
  group:gateways:
    - emnt.dev
tagOwners:
  tag:shared:
    - eaglesemanation # TODO: Replace with autogroup:members when possible
autoApprovers:
  routes:
    192.168.25.0/24:
      - emnt.dev
  exitNode:
    - group:gateways
