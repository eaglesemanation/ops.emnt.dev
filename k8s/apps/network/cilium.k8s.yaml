apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: cilium
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/apps/network/cilium
  prune: true
  sourceRef:
    kind: GitRepository
    name: emnt-ops
  wait: true
