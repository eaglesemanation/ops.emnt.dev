apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: minecraft

resources:
  - rcon.sops.yaml

configMapGenerator:
  - name: vault-config
    options:
      disableNameSuffixHash: true
    files:
      - bluemap.conf
      - unified-metrics-prometheus.yaml
