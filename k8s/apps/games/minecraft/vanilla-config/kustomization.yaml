apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: minecraft

resources:
  - rcon.sops.yaml

configMapGenerator:
  - name: vanilla-config
    options:
      disableNameSuffixHash: true
    files:
      - bluemap-bannermarkers.yaml
      - bluemap.conf
      - geyser.yaml
      - no-chat-reports.yaml
      - vane-admin.yaml
      - vane-permissions.yaml
      - unified-metrics-prometheus.yaml
