java:
  servers:
    vanilla:
      gateways:
        - default
      domains:
        - vanilla.mc.${CLUSTER_DOMAIN}
      address: vanilla-minecraft.minecraft.svc.cluster.local:25565
bedrock:
  servers:
    default:
      gateways:
        - default
      domains:
        - vanilla.mc.${CLUSTER_DOMAIN}
      address: vanilla-minecraft-bedrock.minecraft.svc.cluster.local:19132
