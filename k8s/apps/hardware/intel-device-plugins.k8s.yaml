apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: intel-device-plugins
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/apps/hardware/intel-device-plugins
  prune: true
  sourceRef:
    kind: GitRepository
    name: emnt-dev
  dependsOn:
    - name: node-feature-discovery
  wait: true
