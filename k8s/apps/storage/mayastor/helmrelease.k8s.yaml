apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mayastor
  namespace: mayastor
spec:
  interval: 30m
  chart:
    spec:
      chart: mayastor
      version: 2.0.0
      sourceRef:
        kind: HelmRepository
        name: openebs-mayastor
        namespace: flux-system
      interval: 30m
