apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: recyclarr
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/apps/multimedia/recyclarr
  prune: true
  sourceRef:
    kind: GitRepository
    name: emnt-ops
  wait: true
