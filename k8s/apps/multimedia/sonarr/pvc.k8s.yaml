apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: multimedia
  namespace: sonarr
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Ti
  storageClassName: node-manual
  selector:
    matchLabels:
      share: multimedia
