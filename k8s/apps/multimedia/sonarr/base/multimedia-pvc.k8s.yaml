apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: multimedia-sonnar
  labels:
    app.kubernetes.io/name: sonarr
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Ti
  selector:
    matchLabels:
      app.kubernetes.io/name: sonarr
